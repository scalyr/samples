{
  // specify a time zone if the timestamps in your log are not in GMT
  // timezone: "GMT-0800"

  formats: [
    {
      format: "$timestamp$ $elapsed=number$ $remotehost$ $code$/$status$ $bytes$ $method$ $url$ $rfc931$ $peerstatus$/$peerhost$ $type$"
    }
  ]
}