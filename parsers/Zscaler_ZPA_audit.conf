{
  // specify a time zone if the timestamps in your log are not in GMT
  // timezone: "GMT-0800"
  
  formats: [
    "Audit zpa-lss: ,$modifiedTime$,$creationTime$,$modifiedBy$,$requestId$,$sessionId$,$auditOldValue$,$auditNewValue$,$auditOperationType$,$objectType$,$objectName$,$objectId$,$customerId$,$modifiedByUser$,$isClientAudit$"
  ]
}

