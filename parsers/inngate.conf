{
  // specify a time zone if the timestamps in your log are not in GMT
  // timezone: "GMT+0800"

  formats: [
    {
     format: "\\\"$timestamp$\\\", \\\"$device$\\\","
    }
    {
    id: "inngate",
    format : ".*TM=$TM$ IF=$IF$ OF=$OF$ IFS=$IFS$ UID=$UID$ BID=$BID$ MAC=$MAC$ PRO=$PRO$ OSA=$OSA$ ODA=$ODA$ SA=$SA$ DA=$DA$ MET=$METHOD$ HOST=$HOST$ UA=$UA$ URI=$URI$\\\""
    }
  ]
}
